.sim-search-panel
  center
    input.b-searchbar(type="text" placeholder="search..." ng-model="simSearchPanelCtrl.simSearchTerms")
  .sim-item(*ngFor='#sim of sims')
      center
        img(src="{{sim.img}}")
        p {{sim.name}}
