.row
  .col-xs-10.col-xs-offset-1
      input.b-searchbar(
          #searchTerm
          type='text',
          placeholder='search...',
          aria-describedby='basic-addon1',
          (keyup)="updateList(searchTerm.value)"
      )
.row
    ul.ba-chatlist
      .ba-no-glow(*ngFor='#conversation of conversations')
        list-item([conversation]='conversation')
